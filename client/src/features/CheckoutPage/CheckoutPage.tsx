import { Typography } from "@mui/material";
import { Fragment } from "react";

export default function CheckoutPage() {
    return(
    <Fragment>
        <Typography sx={{mt : 10, fontSize : '30px'}}>This should be seen by the logged in users</Typography>
    </Fragment>
    )
}